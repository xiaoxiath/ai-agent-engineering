# 第 22 章：Agent 体验设计 — 代码示例

> 完整代码示例将从正文重构中迁移至此。

## 状态

本目录为占位目录。随着各章节正文内容的稳定，内联代码将被提取为独立可运行的示例文件，并统一引用 `shared/` 中的公共类型与工具函数。

## 引用方式

```typescript
import { type Message, type AgentState, estimateTokens } from '../shared';
```
